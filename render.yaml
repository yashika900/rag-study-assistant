services:
  - type: web
    name: rag-study-assistant-backend
    env: python
    plan: free

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      uvicorn backend.app.main:app --host 0.0.0.0 --port 10000

    envVars:
      - key: GEMINI_API_KEY
        sync: false